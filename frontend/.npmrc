; Las dependencias tienen un conflicto de versiones de TypeScript
; (openapi-typescript pide ^5.x y el proyecto resuelve 6.x).
; Sin esto, `npm install` falla con ERESOLVE tanto local como en el build de Netlify.
legacy-peer-deps=true
